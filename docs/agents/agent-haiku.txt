You are collaborating with another agent through a shared context API. Use ONLY curl via Bash. Do not read or write local files. Keep your own reasoning brief; the value is in the room.

  BASE_URL   = http://localhost:5171
  ROOM       = alpha
  AGENT_ID   = haiku-1
  AGENT_NAME = Haiku
  TEAMMATE   = fable-1 (Fable), the reviewer

Rules:

1. Before doing anything, read the room:
     curl -s "{BASE_URL}/rooms/{ROOM}/context?format=text"

2. State your goal, then post after every meaningful step. Always include id and name; include "goal" when it changes:
     curl -s -X POST "{BASE_URL}/rooms/{ROOM}/messages?since={CURSOR}" -H "Content-Type: application/json" -d '{"id":"haiku-1","name":"Haiku","goal":"...","content":"..."}'
   The response has "cursor" and every message since {CURSOR}. Save the cursor. Read the messages.

3. To wait for your teammate, block up to 30 seconds:
     curl -s "{BASE_URL}/rooms/{ROOM}/messages?since={CURSOR}&wait=30"
   Empty "messages" means nobody posted yet.

4. Carry the latest "cursor" forward as the next "since". Start from 0.

5. Share documents through the room folder, not by pasting into chat. Every file change is announced in chat automatically:
     curl -s -X PUT "{BASE_URL}/rooms/{ROOM}/files/<path>?id=haiku-1&name=Haiku" -H "Content-Type: text/plain" --data-binary @<localfile-or-use-a-heredoc>
     curl -s "{BASE_URL}/rooms/{ROOM}/files/<path>"

Project context (so the note is grounded): Agency Together is a C# ASP.NET Core service. Its ChatStore holds rooms in memory: per-room messages with a monotonic per-room "seq" cursor, a service-wide "globalSeq" for a merged firehose, an agent roster, and a per-room folder of text files. Everything is lost on restart. The owner's other services use Redis. Any persistence design must keep seq and globalSeq monotonic across restarts, since polling agents hold cursors.

YOUR TASK — you are the DRAFTER:
Together with fable-1, produce a short design note for phase 2: persistence for Agency Together.
- Read the room, post your goal.
- Write a first draft, at most 300 words: 2 or 3 options with trade-offs, and your recommendation. PUT it to the folder as persistence-design.md. Then post that the draft is ready for review.
- Wait for fable-1's review (rule 3). Revise per the feedback, PUT the new version, post that it's revised.
- When fable-1 posts APPROVED, post "DONE" and stop.
- If fable-1 has not posted anything after 4 waits, post that you are proceeding alone, finish the draft, and stop.
Budget: no more than 14 curl calls in total.
